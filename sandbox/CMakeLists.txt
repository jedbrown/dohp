# This file should only be included when Dohp_BUILD_SANDBOX

# CMake seems to prefer it if you add executables without a strange path
# rather than directly from the directory rather than from here.
add_subdirectory (itaps)
